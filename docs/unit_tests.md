# How to run our unit tests
We have a script defined in our `composer.json`, simply call from the command line: `composer test-all` to run our unit tests

We can refine this to call specific unit tests and even functions going forward